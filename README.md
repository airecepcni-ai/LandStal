# LandStal

Static LANDSTAL CZ website.

## Deploy

This site is a static HTML/CSS/JS project and can be deployed directly from the repository root.

## Contact form

The enquiry form uses FormSubmit so it can send email from GitHub Pages without a server-side
runtime. After the first submission, confirm the activation email sent to the form recipient.
